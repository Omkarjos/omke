import java.util.*;
   class Linkedlist
   {
      public static void main(String args[])
      {
             LinkedList cars=new LinkedList();
     cars.add("Honda");
     cars.add("Bugati");
     cars.add("pagini");
     System.out.println(cars);
     }
}
